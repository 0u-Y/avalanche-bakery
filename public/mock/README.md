# Mock image slots

The display expects optional event assets at these paths:

- `cookie-1.jpg` through `cookie-6.jpg`
- `certificate-1.jpg` through `certificate-6.jpg`

When a file is absent, the display automatically uses the palette-based cookie or certificate placeholder.
